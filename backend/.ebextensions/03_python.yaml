container_commands:
  01_migrate:
    command: "source /opt/python/run/venv/bin/activate && python conduit/manage.py migrate --noinput"

option_settings:
  "PYTHONPATH": "/opt/python/current/app/conduit:$PYTHONPATH"
  aws:elasticbeanstalk:container:python:
    WSGIPath: conduit/wsgi.py
  